import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    backgroundColor: 'tomato',
    color: '#FFFFFF',
    alignItems: 'center',
    borderRadius: 25,
    margin: 10
  }
})